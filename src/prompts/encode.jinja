## Instruction
You are a domain-specific language (DSL) specialist.
Your task is to transform the following normative scenario into a precise specification written in the DSL defined below.

{% if ablation.syntax %}
## Context
- **DSL Name**: `{{ dsl.name }}`
- **JSON Schema**:

{{ schema }}
{% endif %}

{% if legenda %}
## Vocabulary (use these labels consistently)

The following canonical labels were extracted for this scenario. When the schema lets you choose identifier-like values (variable names, IDs, action names, role names), prefer the labels below. Do not invent new labels for concepts already listed.

{{ legenda }}
{% endif %}

## Input Scenario

{{ scenario }}

## Output Requirements
1. **Format** - Produce a single JSON document that conforms **exactly** to the JSON schema above.
2. **No extra text** - Do **not** add comments, explanations, or surrounding code fences.
3. **Validity** - The output must parse as JSON and validate against the schema.
4. **Precision** - If the scenario contains ambiguous or missing details, use placeholders (e.g., `__UNKNOWN__`) but *do not* invent values.

{% if ablation.few_shot %}

{% for example in examples %}
## Example (for reference only)
**Example Scenario**: `{{ example.input }}`
**Expected Output**:

{{ example.output }}

{% endfor %}

{% endif %}

{% if attempt %}
## Your previous attempt was rejected by the JSON-schema validator

Previous output:

{{ attempt.previous }}

Validator feedback:

{{ attempt.error }}

Produce a corrected JSON document that addresses the feedback above.
{% endif %}

## Final Output
Respond **only** with the JSON specification.
Do **not** add any surrounding Markdown, code fences, or explanatory text.
