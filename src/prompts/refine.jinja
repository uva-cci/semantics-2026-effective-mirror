## Your previous attempt was rejected by the JSON-schema validator

Previous output:

{{ previous }}

Validator feedback:

{{ error }}

Produce a corrected JSON output that addresses the feedback above.
